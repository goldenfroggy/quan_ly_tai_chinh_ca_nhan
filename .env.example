# Mẫu cấu hình. Copy sang .env rồi chỉnh nếu cần.
# Mặc định (không cần file này) đã dùng PostgreSQL qua docker-compose.
DATABASE_URL=postgresql+psycopg2://finance_user:finance_password@localhost:5432/finance_db
